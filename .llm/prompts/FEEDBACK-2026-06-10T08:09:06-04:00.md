Feedback from a preview user:

> You will probably need a bit more to make this workable. Imagine running this as a service. Some mods will fail or some will spam too many reviews. You need a way to manage the mods and for authors to manage their own mods
> Also consider authorship, when you create a mod you need to be able to test it

I would like all of the spec/example spec/example impl be re-worked to integrate this.
Eg: there should be some notion of a service and a testing workflow.
Ideally the business logic is entirely shared between the daemon and the cli apps.

One possible authoriship flow is execute the codemods against a test repo/test pr review system (or otherwise mark the
resulting pr's as drafts), personal owner email address/slack dm/channel, ...
